"""
URL конфигурация для приложения greeting_app.

Этот файл определяет маршруты (URL patterns) для приложения.
Каждый маршрут связывает URL адрес с представлением (view).
"""

from django.urls import path
from . import views

# Имя приложения для использования в пространствах имен URL
# Позволяет избежать конфликтов имен URL между приложениями
app_name = 'greeting_app'

# Список URL паттернов приложения
urlpatterns = [
    # Маршрут для главной страницы
    # '' - пустая строка означает корневой URL (главная страница)
    # views.home - функция представления, которая обработает запрос
    # name='home' - имя маршрута для использования в шаблонах и redirect()
    path('', views.home, name='home'),
]

